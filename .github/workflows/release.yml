# Release: publish a single gem to RubyGems via tag. Uses Trusted Publishing (OIDC).
# Tag format: <gem_name>@<version>  e.g. cpf-dv@1.0.1 or br-utilities@2.1.0
# Only the tagged gem is built and pushed; dependencies must already be on RubyGems.
# The version is derived from the tag/input and written into the package's version.rb
# at release time, so committing a version bump before tagging is NOT required.
name: Release

on:
  workflow_dispatch:
    inputs:
      gem_name:
        description: "Gem name (RubyGems, e.g. cpf-dv)"
        required: true
        type: string
      version:
        description: "Version (semver, e.g. 1.0.1)"
        required: true
        type: string
  push:
    tags:
      - "*@*"   # e.g. cpf-dv@1.0.1

jobs:
  validate:
    name: Validate
    runs-on: ubuntu-latest
    outputs:
      gem_name: ${{ steps.vars.outputs.gem_name }}
      version: ${{ steps.vars.outputs.version }}
      tag: ${{ steps.vars.outputs.tag }}
      dir: ${{ steps.vars.outputs.dir }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0
      - id: vars
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            GEM_NAME="${{ github.event.inputs.gem_name }}"
            VERSION="${{ github.event.inputs.version }}"
          else
            TAG="${GITHUB_REF#refs/tags/}"
            GEM_NAME="${TAG%%@*}"
            VERSION="${TAG#*@}"
          fi
          DIR="${GEM_NAME}"
          [ -n "$DIR" ] && [ -n "$VERSION" ] || { echo "::error::Could not get gem name and version (event=${{ github.event_name }}, ref=${{ github.ref }})"; exit 1; }
          [ -d "packages/$DIR" ] || { echo "::error::Package packages/$DIR not found"; exit 1; }
          [ -f "packages/$DIR/${GEM_NAME}.gemspec" ] || { echo "::error::Gemspec not found"; exit 1; }
          [[ "$VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+ ]] || { echo "::error::Version must be semver"; exit 1; }
          echo "gem_name=$GEM_NAME" >> $GITHUB_OUTPUT
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "tag=${GEM_NAME}@${VERSION}" >> $GITHUB_OUTPUT
          echo "dir=$DIR" >> $GITHUB_OUTPUT

  test-and-release:
    name: Test & Publish
    needs: validate
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
    environment: release
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0

      - uses: ruby/setup-ruby@09a7688d3b55cf0e976497ff046b70949eeaccfd # v1.288.0
        with:
          ruby-version: "3.2"
          bundler-cache: true

      - name: Set version in source from tag
        run: |
          DIR="${{ needs.validate.outputs.dir }}"
          GEM_NAME="${{ needs.validate.outputs.gem_name }}"
          VERSION="${{ needs.validate.outputs.version }}"
          VER_FILE="packages/$DIR/src/${GEM_NAME}/version.rb"
          [ -f "$VER_FILE" ] || { echo "::error::Version file $VER_FILE not found"; exit 1; }
          # Overwrite the VERSION literal in place, keeping the module wrapper intact.
          # This decouples the released version from what's committed: no version-bump
          # commit is required before tagging.
          sed -i -E "s/VERSION = ['\"][^'\"]*['\"]/VERSION = '${VERSION}'/" "$VER_FILE"
          grep -qE "VERSION = '${VERSION}'" "$VER_FILE" || {
            echo "::error::Failed to set version $VERSION in $VER_FILE"
            exit 1
          }
          echo "Set $VER_FILE -> VERSION = '${VERSION}'"

      - name: Run tests
        run: |
          cd "packages/${{ needs.validate.outputs.dir }}"
          bundle install
          bundle exec rake test

      - name: Publish to RubyGems (OIDC)
        uses: rubygems/release-gem@1c162a739e8b4cb21a676e97b087e8268d8fc40b # v1.1.2
        with:
          await-release: "false"
        env:
          RELEASE_PACKAGE: ${{ needs.validate.outputs.dir }}

      # Tag must already exist (pushed by maintainer). No auto-tag here.
